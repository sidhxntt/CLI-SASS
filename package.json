{
  "name": "coderush",
  "version": "1.0.0",
  "bin": {
    "coderush": "./src/index.ts"
  },
  "license": "MIT",
  "scripts": {
    "start": "ts-node ./src/index.ts",
    "install": "echo 'This package is only meant to be used with npx' && exit 1"
  },
  "dependencies": {
    "@clack/prompts": "^0.7.0"
  },
  "devDependencies": {
    "ts-node": "^10.9.2",
    "typescript": "^5.5.4"
  }
}
